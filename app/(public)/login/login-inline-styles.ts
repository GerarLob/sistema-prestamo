import type { CSSProperties } from "react";

/** Estilos en línea: el login se ve bien aunque falle el CSS global o Tailwind. */
export const styles = {
  shell: {
    minHeight: "100dvh",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    padding: "1.5rem 0.75rem 2rem",
    position: "relative" as const,
    overflow: "auto",
    backgroundColor: "rgb(240, 249, 255)",
    backgroundImage: [
      "radial-gradient(ellipse 130% 90% at 50% -25%, rgba(125, 211, 252, 0.45) 0%, transparent 50%)",
      "radial-gradient(ellipse 80% 60% at 100% 0%, rgba(59, 130, 246, 0.12) 0%, transparent 45%)",
    ].join(", "),
  } satisfies CSSProperties,
  form: {
    width: "100%",
    maxWidth: "24rem",
    borderRadius: "1.5rem",
    border: "1px solid rgba(125, 211, 252, 0.45)",
    background: "linear-gradient(180deg, rgba(255, 255, 255, 0.85) 0%, rgba(240, 249, 255, 0.45) 100%)",
    boxShadow: "0 8px 32px rgba(15, 23, 42, 0.08), inset 0 1px 0 0 rgba(255, 255, 255, 0.55)",
    padding: "1.5rem 1.25rem",
  } satisfies CSSProperties,
  logo: {
    margin: "0 auto 0.75rem",
    display: "flex",
    width: "3.5rem",
    height: "3.5rem",
    alignItems: "center",
    justifyContent: "center",
    borderRadius: "1.25rem",
    background: "linear-gradient(135deg, #38bdf8, #2563eb)",
    color: "#fff",
    fontSize: "1.125rem",
    fontWeight: 700,
    boxShadow: "0 4px 14px rgba(37, 99, 235, 0.4)",
  } satisfies CSSProperties,
  h1: {
    margin: 0,
    textAlign: "center",
    color: "rgb(15, 23, 42)",
    fontSize: "1.25rem",
    fontWeight: 600,
    letterSpacing: "-0.02em",
  } satisfies CSSProperties,
  sub: {
    margin: "0.35rem 0 0",
    textAlign: "center",
    color: "rgb(71, 85, 105)",
    fontSize: "0.875rem",
  } satisfies CSSProperties,
  err: {
    marginTop: "1.25rem",
    padding: "0.5rem 0.75rem",
    textAlign: "center",
    fontSize: "0.875rem",
    color: "rgb(127, 29, 29)",
    background: "rgba(254, 242, 242, 0.95)",
    border: "1px solid rgba(252, 165, 165, 0.8)",
    borderRadius: "0.75rem",
  } satisfies CSSProperties,
  field: {
    marginTop: "1rem",
  } satisfies CSSProperties,
  label: {
    display: "block",
    marginBottom: "0.35rem",
    fontSize: "0.7rem",
    fontWeight: 500,
    color: "rgb(71, 85, 105)",
  } satisfies CSSProperties,
  input: {
    display: "block",
    width: "100%",
    minHeight: "2.75rem",
    boxSizing: "border-box",
    borderRadius: "0.75rem",
    border: "1px solid rgba(203, 213, 225, 0.9)",
    background: "rgba(255, 255, 255, 0.7)",
    padding: "0.5rem 0.75rem",
    fontSize: "0.95rem",
    color: "rgb(15, 23, 42)",
  } satisfies CSSProperties,
  btn: {
    width: "100%",
    minHeight: "2.75rem",
    marginTop: "1.5rem",
    border: "none",
    borderRadius: "0.75rem",
    background: "linear-gradient(180deg, #3b82f6, #2563eb)",
    color: "#fff",
    fontSize: "0.9rem",
    fontWeight: 500,
    cursor: "pointer",
    boxShadow: "0 4px 14px rgba(37, 99, 235, 0.3)",
  } satisfies CSSProperties,
} as const;
